{
  "name": "genai",
  "version": "1.0.0",
  "description": "```\r nagarik/\r ├── index.html       ← Main UI (open in browser)\r ├── styles.css       ← All styles\r ├── app.js           ← Frontend logic\r ├── server.js        ← Node.js backend\r ├── .env             ← Your API key (create this)\r └── package.json     ← (auto-created by npm init)\r ```",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1"
  }
}
